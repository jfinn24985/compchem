class matcher_regex
!!!1297065.cpp!!!	process_part(in a_target : string, out a_result : string) : int
// LOCAL
boost::smatch l_result;
// DO
if (boost::regex_search (a_target, l_result, finder))
{
  a_result.assign (l_result.format (format));
}
return CCHEM_SUPPLIER_SUCCESS;
