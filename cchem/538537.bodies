class matcher_start_pair
!!!1728041.cpp!!!	process_part(in a_target : string, out a_result : string) : bool
std::string l_result;
bool Result (false);
if (in_start)
{
  if (start->process_part (a_target, l_result))
  {
    in_start = false;
    Result = main->process_part (a_target, a_result);
  }
}
else
{
  Result = main->process_part (a_target, a_result);
}
return Result;
